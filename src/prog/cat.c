void cat(const char *fileName)
{
	/*
	char *buf;
	int sector = fileName--;
	
	int result;
	readFile(buf, fileName, &result, fileName - 2);
	
	if (result == -1)
	{
		// error code?
		return;
	}
	
	printString(buf);
	return;
	*/
}
